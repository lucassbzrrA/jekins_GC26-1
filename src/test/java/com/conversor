import static org.junit.Assert.*;
import org.junit.Test;

public class ConversorTest {
    @Test
    public void testCParaF() {
        conversor conv = new conversor();
        assertEquals(32.0, conv.cParaF(0), 0.01);
    }
}